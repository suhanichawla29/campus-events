(function() {    var savedTheme = localStorage.getItem("theme");    var themeButton = document.getElementById("theme-btn");    if (savedTheme === "dark") {        document.body.classList.add("dark-mode");        if (themeButton) themeButton.innerHTML = "☀️";    } else {        document.body.classList.remove("dark-mode");        if (themeButton) themeButton.innerHTML = "🌙";    }})();function toggleTheme() {    document.body.classList.toggle("dark-mode");    var themeButton = document.getElementById("theme-btn");    if (document.body.classList.contains("dark-mode")) {        localStorage.setItem("theme", "dark");        if (themeButton) themeButton.innerHTML = "☀️";    } else {        localStorage.setItem("theme", "light");        if (themeButton) themeButton.innerHTML = "🌙";    }}function validateRegisterForm() {    var name = document.getElementById("reg-name").value.trim();    var email = document.getElementById("reg-email").value.trim();    var password = document.getElementById("reg-password").value;    var confirm = document.getElementById("reg-confirm").value;    if (name == "") {        alert("Please enter your full name.");        return false;    }    if (email == "") {        alert("Please enter your email.");        return false;    }    if (email.indexOf("@") == -1 || email.indexOf(".") == -1) {        alert("Please enter a valid email address.");        return false;    }    if (password.length < 6) {        alert("Password must be at least 6 characters long.");        return false;    }    if (password != confirm) {        alert("Passwords do not match.");        return false;    }    return true;}function validateLoginForm() {    var email = document.getElementById("login-email").value.trim();    var password = document.getElementById("login-password").value;    if (email == "") {        alert("Please enter your email.");        return false;    }    if (password == "") {        alert("Please enter your password.");        return false;    }    return true;}(function() {    var cancelModal = document.getElementById("cancel-modal");    if (!cancelModal) {        return;    }    var overlay = cancelModal;    var closeButton = document.getElementById("cancel-modal-close");    var keepButton = document.getElementById("cancel-keep");    var cancelForm = document.getElementById("cancel-form");    var selectedEventId = document.getElementById("cancel-event-id");    var selectedEventName = document.getElementById("cancel-event-title");    var selectedEventDate = document.getElementById("cancel-event-date");    var lastFocused = null;    var focusableSelector = "button, [href], input, select, textarea, [tabindex]:not([tabindex='-1'])";    function focusableElements() {        return Array.prototype.filter.call(            cancelModal.querySelectorAll(focusableSelector),            function(element) { return !element.disabled && element.offsetParent !== null; }        );    }    function openModal(id, title, date) {        selectedEventId.value = id;        selectedEventName.textContent = title;        selectedEventDate.textContent = date;        lastFocused = document.activeElement;        cancelModal.classList.add("show");        document.body.classList.add("modal-open");        var first = focusableElements()[0];        if (first) {            first.focus();        }    }    function closeModal() {        cancelModal.classList.remove("show");        document.body.classList.remove("modal-open");        if (lastFocused) {            lastFocused.focus();        }    }    document.addEventListener("click", function(event) {        var trigger = event.target.closest(".js-cancel-trigger");        if (trigger) {            event.preventDefault();            var id = trigger.getAttribute("data-event-id");            var title = trigger.getAttribute("data-event-title");            var date = trigger.getAttribute("data-event-date");            openModal(id, title, date);            return;        }        if (event.target === overlay) {            closeModal();        }    });    if (closeButton) {        closeButton.addEventListener("click", closeModal);    }    if (keepButton) {        keepButton.addEventListener("click", closeModal);    }    document.addEventListener("keydown", function(event) {        if (!cancelModal.classList.contains("show")) {            return;        }        if (event.key === "Escape") {            event.preventDefault();            closeModal();            return;        }        if (event.key === "Tab") {            var list = focusableElements();            if (list.length === 0) {                return;            }            var first = list[0];            var last = list[list.length - 1];            if (event.shiftKey && document.activeElement === first) {                event.preventDefault();                last.focus();            } else if (!event.shiftKey && document.activeElement === last) {                event.preventDefault();                first.focus();            }        }    });})();